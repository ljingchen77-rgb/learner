# 网站公开信息与前端安全审计

审计日期：2026-08-12

## 结论摘要

当前仓库和线上网站未发现密码、API 密钥、访问令牌、私钥、手机号、QQ 邮箱或本地磁盘路径泄露。公开信息包括姓名、教育与研究经历、GitHub 账号、个人网站和 Outlook 联系邮箱；这些是个人主页的预期内容，但应视为可被搜索引擎、爬虫和第三方长期保存的信息。

## 中风险

### SEC-01 Markdown 安全组件加载失败时曾降级为未清理 HTML

- 位置：`blog/blog.js` 的 `loadPost`
- 原行为：DOMPurify 未加载时仍将 Marked 生成的 HTML 写入 `innerHTML`。
- 影响：如果仓库文章内容被恶意修改，同时 DOMPurify 加载失败，访问文章的浏览器可能执行注入内容。
- 修复：安全组件缺失时停止渲染并显示错误；只有 DOMPurify 可用时才写入清理后的 HTML。

## 低风险与加固建议

### SEC-02 CDN 脚本缺少完整性校验

- 位置：`blog/index.html` 中的 Marked 与 DOMPurify CDN 脚本。
- 影响：第三方 CDN 被篡改时，脚本拥有与网站前端相同的执行权限。
- 建议：固定 Marked 版本并为两个脚本增加 SRI，或将已审计版本放入仓库自托管。

### SEC-03 未配置内容安全策略

- 位置：HTML 页面和 GitHub Pages 响应中未见 CSP。
- 影响：CSP 不能替代输入清理，但缺少它会降低 XSS 的纵深防护能力。
- 建议：先移除主页内联脚本，再通过页面早期的 CSP meta 标签限制脚本、样式和连接来源。GitHub Pages 无法由本仓库直接设置全部安全响应头。

### SEC-04 Utterances 属于第三方脚本

- 位置：`blog/blog.js` 的 `loadComments`
- 影响：评论组件可以在页面中执行脚本，并向 GitHub/Utterances 发出请求。
- 建议：继续限定固定来源；若不需要评论，移除该组件可进一步减少第三方依赖。

## 公开信息清单

- 姓名：刘靖臣
- 学校、院系、培养去向及研究方向
- GitHub 用户名与个人主页地址
- Outlook 联系邮箱 `jingchen0911@outlook.com`
- 毕业论文与受控合成研究的摘要、方法和结果

未发现公开：手机号、QQ 邮箱、身份证号、家庭住址、生日、密码、令牌、私钥、本地 Windows 路径或原始研究数据。

## 隐私说明

GitHub 仓库和 GitHub Pages 均为公开发布渠道。即使日后删除某项内容，也不能保证搜索引擎缓存、仓库克隆或第三方存档同步删除。对外发布前应避免加入个人手机号、身份证明、家庭地址、未授权机构材料、受限数据及尚未公开的敏感研究细节。
